{
  "name": "server",
  "version": "1.0.0",
  "description": "api for contact app",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "author": "Nameesh",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
